# UC-324 — Protocolo de Contención de Sandbox
Flask==3.0.3
requests==2.32.2
pytest==8.2.1

# Microsoft Agent Governance Toolkit (A) — Phase 1 governance, rings, SRE
agent-governance-toolkit[full]==4.1.0

# AdaptiveStressTestingToolbox (B) — NOT installable as-is.
# The upstream repo depends on garage==2019.10.1 -> torch==1.3.0, which is
# unavailable for Python 3.11 / arm64. UC-324 implements the AST core algorithm
# (MCTS-UCT for worst-case discovery) natively in ast_integration.py.
# ai-safety (C) — NOT installable as a pip package.
# The repo has multiple top-level packages without a valid setuptools configuration,
# so `pip install git+...` fails with "Multiple top-level packages discovered".
# UC-324 implements a native red-teaming engine in ai_safety_integration.py.
# SafeAuto (D) — NOT installable as a pip package.
# The repo has no setup.py or pyproject.toml, so pip install fails.
# UC-324 implements a native SafeAuto-style post-action rule engine in
# safeauto_integration.py.
# awesome-safety-critical-ai (E) — NOT a Python package; it is a curated
# resource list. UC-324 implements the circuit breaker / confidence threshold /
# safety-critical monitoring patterns in safety_critical_monitor.py.
# faramesh-core (F) — NOT installable as a pip package.
# The repo has no setup.py or pyproject.toml. UC-324 implements the
# cryptographic/deterministic boundary pattern natively in faramesh_boundary.py.
# agent-policy-engine (G) — NOT installable as a pip package.
# The repo has no setup.py or pyproject.toml. UC-324 implements a native
# prompt-injection policy engine with provenance, detectors, score and fail-closed
# behavior in prompt_injection_policy_engine.py.
# agent-guardrails (H) — NOT installable as a pip package.
# The repo has no setup.py or pyproject.toml. UC-324 implements native
# DevOps/SRE/Kubernetes/IaC guardrails in devops_guardrails.py.
# openguardrails (I) — NOT installable as a pip package from the monorepo.
# The repository is a protocol/spec monorepo with multiple top-level directories
# and no installable Python package metadata at the root, so `pip install git+...`
# fails with "Multiple top-level packages discovered in a flat-layout".
# UC-324 implements native/fallback LLM traffic guardrails in llm_guardrails.py
# inspired by the OpenGuardrails GuardEvent/Verdict contract.
# AgentDoG (J) — NOT installable as a pip package in this environment.
# The repository is a model-based framework (AgentDoG 1.0/1.5) that requires
# HuggingFace/Transformers checkpoints and prompt templates; it has no pip
# installable package metadata at the root. UC-324 implements a native
# deterministic trajectory evaluator in agent_dog_integration.py inspired by
# the AgentDoG safety taxonomy and trajectory diagnosis concepts.
# OpenAgentSafety (K) — NOT installable as a pip package in this environment.
# The repository is a Docker-based benchmark of simulaciones realistas de
# alto riesgo que requiere servicios (GitLab, ownCloud, RocketChat, etc.) y
# ~30 GB de disco. UC-324 implementa un evaluador determinista por etapas en
# open_agent_safety_integration.py inspirado en las estrategias rule-based y
# LLM-as-Judge de OpenAgentSafety.
