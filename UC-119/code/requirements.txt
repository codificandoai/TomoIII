# Codificando.AI - UC-119
# Dependencias del pipeline de monitoreo de LLMs

# Núcleo
numpy>=1.24,<2.0
flask>=3.0,<4.0

# Métricas / observabilidad
prometheus-client>=0.19,<1.0

# Testing
pytest>=7.4,<9.0
pytest-cov>=4.1,<6.0

# --- Dependencias opcionales (activadas via LLM_USE_ML_MODELS=true) ---
# torch>=2.1
# transformers>=4.36

# --- Trazabilidad opcional (activada via MLFLOW_ENABLED=true) ---
# mlflow>=2.9,<3.0

# --- Tracing opcional (activado via TRACING_ENABLED=true) ---
# opentelemetry-api>=1.22
# opentelemetry-sdk>=1.22
# opentelemetry-exporter-otlp-proto-http>=1.22

# --- Servidor WSGI de producción ---
gunicorn>=21.2,<23.0
