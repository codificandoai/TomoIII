# TomoIII
## AI-Native Operations & Agentic Patterns
### CASE: UC-075 — Continuous Training Orchestrator

#### USO: INTERNO

UC-075 es una **capa externa** del ecosistema AGI que orquesta el reentrenamiento continuo de modelos desplegados en producción sin modificar los motores internos de otras UCs. Unifica:

- 4 estrategias de entrenamiento.
- 5 gates de validación para despliegues por lotes.
- Aprendizaje online incremental con **Circuit Breaker** y cuarentena de micro-lotes.
- Auditoría inmutable, MLflow/Prometheus/Grafana y aprobación humana (UC-290).

#### EXECUTION
```bash
cd /Users/utron/Documents/code-books/TomoIII/UC-075/code
python -m pytest tests_075 -q        # tests
python api_075.py                     # servicio REST en http://127.0.0.1:5075
python api_075.py --mlflow-uri http://localhost:5000
```

# DESCRIPTION

UC-075 responde a la pregunta: *¿cómo actualizamos modelos en producción de forma segura, justificada, observable y reversible?*

![Arquitectura UC-075](diagrama_interconexion_completo_uc075_uc162_uc290_uc300_uc308_uc309_uc317_uc330_uc087.png)

## Arquitectura batch (reentrenamiento por lotes)

```text
Trigger (SCHEDULED | EVENT_DRIVEN | ON_DEMAND | INCREMENTAL)
    ↓
TriggerEngine — política + presupuesto (fail-closed)
    ↓
VersionFreezeRegistry — dataset versionado con hash
    ↓
Trainer inyectado (UC-087 sandbox) → Candidato
    ↓
Evaluator inyectado (UC-308 out-of-sample) → ChampionCandidate
    ↓
GatePipeline (5 gates obligatorios, fail-fast)
    1. drift gate (UC-308 drift / olvido catastrófico incremental)
    2. champion_challenger gate (UC-308 / UC-087)
    3. security_fairness gate (UC-087 robustez, sesgo, backdoor, PII)
    4. hitl gate (UC-290 aprobación humana en alto impacto)
    5. canary gate (UC-320 / monitor inyectado)
    ↓
Decision: PROMOTE | REJECT | ROLLBACK | PENDING_HITL
    ↓
ChampionRegistry + Rollback automático + MLflowAdapter + Observability075 + AuditLog
```

## Arquitectura online (aprendizaje incremental con Circuit Breaker)

```text
Micro-lote entrante
    ↓
Integrity checkpoint — schema, tipos, nulos, rangos, tamaño
    ↓
Circuit Breaker — CLOSED | OPEN | HALF_OPEN
    ↓
Pre partial_fit concept drift — accuracy vs baseline
    ↓
partial_fit(model, X, y) con checkpoint previo
    ↓
Post partial_fit concept drift — detecta olvido / degradación
    ↓
Si OK → modelo productivo actualizado
Si FALLO → revertir pesos, cuarentenar lote, alertar Prometheus/Grafana
```

## Componentes

| Archivo | Responsabilidad |
|---|---|
| `models_075.py` | Dataclasses: triggers, gates, runs, decisiones, auditoría NIST/ISO. |
| `trigger_engine.py` | 4 estrategias y gobernanza de presupuesto/cap. |
| `gate_pipeline.py` | 6 gates inyectables con fallo-cerrado (incluye explainability_gate). |
| `registry_075.py` | Freeze + hash de datasets; registro campeón/candidato; rollback. |
| `mlflow_adapter_075.py` | MLflow real o fallback JSONL local (no bloqueante); log de cuarentenas. |
| `observability_075.py` | Métricas Prometheus + exports Loki/Tempo + dashboard Grafana. |
| `continuous_training_orchestrator.py` | Orquestador central con inyección de dependencias. |
| `online_incremental_learner.py` | `partial_fit` online + Circuit Breaker + cuarentena de micro-lotes. |
| `regulated_model_governance.py` | Gobernanza sectorial, explicabilidad LIME/SHAP, baseline interpretable, requisitos stakeholders. |
| `api_075.py` | API REST Flask + card views de entrada/salida. |
| `tests_075/` | Tests unitarios e integración. |

## Estrategias de reentrenamiento

| Estrategia | Cuándo | Control |
|---|---|---|
| `SCHEDULED` | Mantenimiento periódico (intervalo configurable). | `check_scheduled(agent_id, interval_hours)` |
| `EVENT_DRIVEN` | drift_score, error_rate, latency_degradation_pct, calibration_error, user_satisfaction, business_kpi_breach superan umbral. | `evaluate_metrics(...)` |
| `ON_DEMAND` | Cambio de producto, política, fuentes de datos o incidente. | `on_demand(...)` |
| `INCREMENTAL` | Mini-batch continuo con replay buffer ≥20% para evitar olvido catastrófico. | `incremental_step(...)` |
| `ONLINE` | Micro-lotes de alta frecuencia con `partial_fit` y Circuit Breaker. | `online_fit(...)` / `OnlineIncrementalLearner` |

## Gates obligatorios (pipeline batch)

```text
drift gate → champion_challenger gate → security_fairness gate → explainability gate → hitl gate → canary gate
```

- **FAIL en cualquier gate** detiene el run.
- **FAIL en canary** produce **rollback automático** al baseline.
- **REQUIRES_HITL** pausa el run hasta aprobación/rechazo humano.

## Circuit Breaker (pipeline online)

| Estado | Comportamiento |
|---|---|
| `CLOSED` | Acepta micro-lotes y aplica `partial_fit` tras validaciones. |
| `OPEN` | Rechaza actualizaciones; cuarentena todos los micro-lotes. |
| `HALF_OPEN` | Permite un número limitado de micro-lotes; si fallan vuelve a OPEN, si tienen éxito cierra el circuito. |

Transiciones:

- `CLOSED` → `OPEN`: fallo de integridad, error en `partial_fit`, o concept drift post-fit.
- `OPEN` → `HALF_OPEN`: transcurrido `cooldown_seconds`.
- `HALF_OPEN` → `CLOSED`: `half_open_max_batches` consecutivos exitosos.

## Gobernanza regulada para sectores críticos

### Sectores soportados

| Dominio | Controles automáticos (ejemplos) |
|---|---|
| `healthcare` | HITL, interpretabilidad, fairness, human override, audit trail |
| `finance` | HITL, interpretabilidad, fairness by subgroup, human override |
| `insurance` | HITL, interpretabilidad, fairness, audit trail |
| `critical_infrastructure_energy` | HITL, interpretabilidad, shadow, redundancia, rollback tested, latencia/disponibilidad |
| `critical_infrastructure_water` | Idem. |
| `critical_infrastructure_telecom` | Idem. |
| `critical_infrastructure_transport` | Idem. |
| `critical_infrastructure_nuclear` | Idem + controles más estrictos de latencia/disponibilidad |
| `critical_infrastructure_industrial` | HITL, interpretabilidad, redundancia, adversarial robustness |
| `general` | Audit trail, rollback tested |

### Baseline interpretable first

`RegulatedModelSelector` siempre evalúa primero un **baseline interpretable** (regresión lineal/logística, árbol pequeño, reglas). Si un sector exige interpretabilidad y no hay candidato interpretable aceptable, se **mantiene el baseline** aunque un black-box tenga mejor accuracy.

### Explainability Gate

Para sectores con `interpretability_required`:

- Modelos interpretables pasan automáticamente.
- Modelos black-box deben superar explicabilidad (LIME, SHAP, surrogate) con:
  - `stability_score >= 0.8`
  - `coverage >= 0.7`
- Si falla, el run se rechaza o se escala a HITL.

### Requisitos de stakeholders

`StakeholderRequirements` captura requisitos innegociables (CSO, CISO, Medical Board, Operaciones). Hasta que no se firmen, el selector bloquea el despliegue y mantiene baseline.

## Métricas Prometheus expuestas

Pipeline batch:

- `uc075_retrain_triggers_total{strategy}`
- `uc075_retrain_completed_total{status}`
- `uc075_gate_verdicts_total{gate,verdict}`
- `uc075_drift_score`
- `uc075_pipeline_status{status}`
- `uc075_retrain_cost_usd`
- `uc075_run_duration_seconds`
- `uc075_pending_hitl`
- `uc075_rollbacks_total`

Online incremental:

- `uc075_circuit_breaker_state{learner}`
- `uc075_quarantined_microbatches_total{learner,reason}`
- `uc075_partial_fit_drift_score{learner,phase}`
- `uc075_online_incremental_accuracy{learner,phase}`

## Auditoría y compliance

Cada run y cada micro-lote cuarentenado se registra en `AuditLog` / MLflow con hash-chain (verificable via `audit_chain_valid`). Datos capturados:

- `strategy_used`
- `drift_score`
- `business_trigger`
- `dataset_version`
- `model_version`
- `code_version`
- `mlflow_run_id`
- `update_status: QUARANTINED` + razón + métricas
- decisión final y aprobador

Alineado con **NIST AI RMF** e **ISO 42001**.

## Integración con el ecosistema

UC-075 **no importa internamente** otros casos de uso. En su lugar, recibe componentes por inyección:

- **UC-087** → `trainer`, `security_fairness`.
- **UC-308** → `drift_detector`, `champion_challenger`.
- **UC-290** → `hitl_approver`.
- **UC-320** → `canary_monitor`.
- **UC-309** → `event_sink` (para ingestión a Prometheus/Loki/Tempo).

Esto evita acoplamiento y preserva la separación de poderes.

# API

La API REST expone card views auto-documentadas en `GET /api/v1/cards`.

![secuencia](diagrama_secuencia_uc075_continuous_training.png)

## Endpoints principales

| Método | Ruta | Descripción |
|---|---|---|
| GET | `/api/v1/health` | Health check. |
| GET | `/api/v1/cards` | Input/output cards de todos los endpoints. |
| POST | `/api/v1/ct/scheduled` | Estrategia SCHEDULED. |
| POST | `/api/v1/ct/event` | Estrategia EVENT_DRIVEN. |
| POST | `/api/v1/ct/on-demand` | Estrategia ON_DEMAND. |
| POST | `/api/v1/ct/incremental` | Estrategia INCREMENTAL. |
| POST | `/api/v1/ct/online-fit` | Micro-lote online con Circuit Breaker. |
| GET | `/api/v1/ct/online-learners` | Estado de todos los learners online. |
| GET | `/api/v1/ct/online-learners/<learner_id>` | Detalle y cuarentena de un learner. |
| POST | `/api/v1/ct/regulatory/domain` | Configura dominio regulatorio y controles. |
| POST | `/api/v1/ct/regulatory/stakeholder-requirement` | Registra requisito innegociable. |
| POST | `/api/v1/ct/regulatory/sign-off` | Firma requisito de stakeholder. |
| POST | `/api/v1/ct/regulatory/select-model` | Selección regulada baseline-first. |
| POST | `/api/v1/ct/regulatory/explainability` | Evalúa explicabilidad de un ModelCard. |
| GET | `/api/v1/ct/regulatory/requirements` | Lista requisitos de stakeholders. |
| POST | `/api/v1/ct/runs/<run_id>/resolve-hitl` | Resuelve PENDING_HITL. |
| GET | `/api/v1/ct/runs` | Lista runs (?status=). |
| GET | `/api/v1/ct/runs/<run_id>` | Detalle de un run. |
| GET | `/api/v1/ct/pending-hitl` | Runs esperando decisión humana. |
| GET | `/api/v1/ct/status` | Estado global. |
| GET | `/api/v1/ct/audit` | Cadena de auditoría. |
| GET | `/api/v1/ct/metrics` | Prometheus exposition. |
| GET | `/api/v1/ct/dashboard` | Dashboard JSON para Grafana. |
| GET | `/api/v1/ct/loki` | Líneas JSON para Loki. |

## Card view de entrada: POST /api/v1/ct/online-fit

```json
{
  "learner_id": "string (requerido)",
  "X": "array de features del micro-lote",
  "y": "array de labels del micro-lote",
  "model_type": "mock | passthrough (solo para pruebas/demo)",
  "policy": {
    "concept_drift_threshold": 0.05,
    "max_quarantine_ratio": 0.20,
    "cooldown_seconds": 60.0,
    "half_open_max_batches": 3,
    "required_schema": ["feature_1", "feature_2"],
    "allowed_nulls": false,
    "feature_ranges": {"feature_1": [0, 100]},
    "max_rows": 10000,
    "eval_metric": "accuracy"
  }
}
```

## Card view de salida: online_microbatch

```json
{
  "batch_id": "string",
  "learner_id": "string",
  "timestamp": 0,
  "integrity": {
    "passed": true,
    "schema_errors": [],
    "null_errors": [],
    "range_errors": [],
    "n_rows": 10,
    "fingerprint": "..."
  },
  "circuit_state_before": "closed|open|half_open",
  "circuit_state_after": "closed|open|half_open",
  "drift": {
    "accuracy_pre": 0.92,
    "accuracy_post": 0.91,
    "drift_score": 0.01,
    "metric_name": "accuracy_drop",
    "threshold": 0.05,
    "breached": false
  },
  "applied": true,
  "reverted": false,
  "quarantined": false,
  "quarantine_reason": "",
  "mlflow_run_id": "...",
  "model_checkpoint_id": "ck-..."
}
```

## Ejemplo: on-demand retraining

```bash
curl -X POST http://localhost:5075/api/v1/ct/on-demand \
  -H "Content-Type: application/json" \
  -d '{
    "agent_id": "medical-triage",
    "change_type": "policy_change",
    "detail": "Nueva guía clínica 2026",
    "new_data": [{"symptoms": "..."}],
    "domain": "medical"
  }'
```

Si el dominio es `medical`, el run quedará en `pending_hitl` hasta que un revisor humano resuelva:

```bash
curl -X POST http://localhost:5075/api/v1/ct/runs/run-xxx/resolve-hitl \
  -H "Content-Type: application/json" \
  -d '{"approved": true, "approver": "dr.smith@hospital.org", "review_notes": "approved after review"}'
```

## Ejemplo: online incremental fit

```bash
curl -X POST http://localhost:5075/api/v1/ct/online-fit \
  -H "Content-Type: application/json" \
  -d '{
    "learner_id": "fraud-detector",
    "X": [[0.5, 0.2], [0.9, 0.8]],
    "y": [0, 1],
    "model_type": "mock",
    "policy": {"concept_drift_threshold": 0.05, "cooldown_seconds": 60}
  }'
```

## Ejemplo: dominio regulatorio + selección de modelo

```bash
# Configurar sector infraestructura crítica (energía)
curl -X POST http://localhost:5075/api/v1/ct/regulatory/domain \
  -H "Content-Type: application/json" \
  -d '{"domain": "critical_infrastructure_energy"}'

# Requisito innegociable de stakeholder
curl -X POST http://localhost:5075/api/v1/ct/regulatory/stakeholder-requirement \
  -H "Content-Type: application/json" \
  -d '{
    "stakeholder": "CISO",
    "description": "Decisiones de control SCADA deben ser explicables",
    "domain": "critical_infrastructure_energy",
    "category": "explainability"
  }'

# Evaluar selección regulada
curl -X POST http://localhost:5075/api/v1/ct/regulatory/select-model \
  -H "Content-Type: application/json" \
  -d '{
    "baseline": {
      "model_id": "logistic-baseline",
      "model_type": "interpretable",
      "algorithm": "logistic_regression",
      "complexity_score": 0.1,
      "metrics": {"auc": 0.82}
    },
    "candidates": [{
      "model_id": "deep-ensemble",
      "model_type": "black_box",
      "algorithm": "xgboost",
      "complexity_score": 0.95,
      "metrics": {"auc": 0.91}
    }]
  }'
```

# Tests

```bash
cd /Users/utron/Documents/code-books/TomoIII/UC-075/code
python -m pytest tests_075 -q
```

Cobertura:

- 4 estrategias de trigger.
- Política de presupuesto y cap diario.
- 5 gates obligatorios (incluyendo rollback automático).
- HITL resolve promote/reject.
- AuditLog hash-chain.
- Observability Prometheus/Loki/Tempo + dashboard.
- Online incremental learner: integrity, drift pre/post, Circuit Breaker, cuarentena, reversión de pesos.
- Regulated model governance: sectores, controles, baseline-first, LIME/SHAP explainability gate, stakeholders.
- API REST Flask card views.

# Notas de gobernanza

- UC-075 nunca promueve un modelo sin que pase todos los gates.
- UC-290 conserva la autoridad humana de aprobación.
- Los datos de entrenamiento se congelan y hashean antes de entrenar.
- El fallback MLflow asegura observabilidad aunque MLflow no esté disponible.
- Prometheus + Grafana proveen tendencias, alertas y trazabilidad regulatoria.
- El Circuit Breaker online evita que ruido, errores de ingesta o cambios conceptuales contaminen el modelo productivo.
